server:
  port: 9296

spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bryanyi/Strings-And-Tones-Config
          clone-on-start: true